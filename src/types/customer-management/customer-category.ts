export interface CustomerCategoryData {
  description: string;
  categoryName: string;
  identity: string;
}
export interface CustomerCategoryFormData {
  description: string;
  categoryName: string;
}
