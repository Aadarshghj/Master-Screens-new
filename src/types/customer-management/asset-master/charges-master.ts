export interface ChargesMasterType {
  chargeName: string,
  gl: string,
  isActive: boolean,
  identity?: string,
}

export interface ChargesMasterData {
  chargeName: string,
  gl: string,
  isActive: boolean,
}
